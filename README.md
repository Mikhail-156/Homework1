# Проект Homework
## Описание:
Проект "Homework" это домашние задание представляет собой набор функций для работы с банковскими операциями.
В проекте есть функции для скрытия номера или счета пользователя, фильтра списка операций, состировку по возрасканию либо по убыванию даты и несколько вспомогательных.

## Установка:

1.Клонируйте репозиторий:

````
git clone https://github.com/Mikhail-156/Homework1.git
````

## Использование:
1. Функция "get_mask_card_number" скрывает номер катры.
Пример номера карты до работы "7000792289606361" после "7000 79** **** 6361"
2. Функция "get_mask_account" скрывает номер счета.
Пример номера счета до работы "73654108430135874305" после "**4305"
3. Функция "mask_account_card" определяет, что ввел пользователь номер или счет, скрывает данные, используя выше перечисленные функции
4. Функция "get_date" меняет формат даты и время.
Пример до работы "2024-03-11T02:26:18.671407" после "11.03.2024".
5. Функция "filter_by_state" возврата словарей банковских операций по ключу.
6. Функция "sort_by_date" сортировки банковских операций по дате.
7. Генератор "filter_by_currency" который фильтрует транзакции по заданной валюте.
8. Генератор "transaction_descriptions" который возвращает описание каждой транзакции по очереди.
9. Генератор "card_number_generator" номера банковских карт в формате ХХХХ ХХХХ ХХХХ ХХХХ где "X" это цифровые номера карты.
10. Декоратор "log" который автоматически логирует начало и конец выполнения функции, а также ее результаты или возникшие ошибки.
11. Функция "convert_to_rub" принимает на вход транзакцию и возвращает сумму транзакции в рублях.
12. Функция "load_transactions" принимает на вход путь до JSON-файла и возвращает список словарей с данными о финансовых транзакциях.
13. Функция "read_csv" для считывания финансовых операций из CSV и возвращения их в виде списка.
14. Функция "read_excel" для считывания финансовых операций из Excel и возвращения их в виде списка.

## Тестирование:
1. Функции "get_mask_card_number", "get_mask_account", "mask_account_card", "get_date" тестируются при помощи параметризации.
2. Функции "filter_by_state", "sort_by_date" тестируются при помощи фикстуров.
3. Генераторы "filter_by_currency", "transaction_descriptions" тестируются при помощи фикстуров.
4. Генератор "card_number_generator" тестируется при помощи параметризации.
#### Все тесты проходять успешно суммарно покрывая больше 80% кода.
